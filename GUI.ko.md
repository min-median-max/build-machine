# 데스크톱 제어 앱

## 범위와 승인 기준

첫 데스크톱 앱은 React 화면과 Rust 명령 연결을 사용하는 macOS용 Tauri 2 앱입니다. 버전은 `0.0.1`입니다. Rust는 기존 `build.py` 제어 명령을 실행하며 Python과 Windows PowerShell이 빌드 작업을 계속 수행합니다. 앱은 이 저장소의 체크아웃, Python, Git, Parallels가 필요합니다. 다른 개발자의 머신을 위한 독립 설치 패키지는 아직 제공하지 않습니다.

앱은 빌드 산출물의 상위 경로나 `~/Work/build-machine`에서 이 체크아웃을 찾습니다. 사용자는 설정의 네이티브 폴더 선택기로 다른 제어 도구 폴더를 고를 수 있습니다. 사이드바에는 등록 프로젝트 목록과 `+`, 하단 **설정** 버튼을 둡니다. 워크스페이스 그룹, 환경 탐색 항목, 별도 프로젝트 메뉴를 두지 않습니다. 최초 화면은 선택한 프로젝트나 등록 안내를 표시합니다. 등록된 프로젝트를 선택하면 해당 빌드 설정과 실행 컨트롤을 바로 엽니다. 설정에는 모든 프로젝트가 공유하는 환경 진단과 도구 준비를 두며 프로젝트는 설치된 도구를 재사용합니다. 빌드에서도 유지보수하는 같은 작업 스크립트로 정의된 누락 항목을 검사하고 설치합니다.

사이드바는 명시적으로 등록한 Git 폴더를 표시합니다. 보이는 `+`로 네이티브 폴더 선택기를 열며, 같은 실제 폴더를 다시 등록하면 기존 항목을 선택합니다. 등록 순서를 유지하고 최근 빌드 로그에 의존하지 않습니다. 프로젝트별 실행 운영체제와 빌드 후 실행 옵션을 저장합니다. 등록 해제는 설정 항목만 없애며 소스·산출물·앱 데이터를 삭제하지 않습니다. 첫 GUI의 기존 설정을 읽을 때 이전에 선택한 프로젝트와 OS 선택을 보존합니다.

공통 제어 도구 경로, 환경 선택, 등록 프로젝트, 프로젝트별 옵션, 현재 프로젝트와 화면은 `~/Library/Application Support/local.buildmachine.desktop/preferences.json`에 유지합니다. 설정 안의 폴더 버튼으로 열어볼 수 있는 이 파일에 접근합니다. 환경의 도구 검사 결과는 제어 도구의 `.state/tool-status.json`에 유지하며 프로젝트들이 공유합니다.

1. 설정된 환경의 실제 VM 연결 상태를 도구 진단과 구분해서 표시합니다. 각 환경의 마지막 진단 또는 도구 준비 결과와 완료 시각을 눈에 띄게 표시하고 앱을 다시 열어도 유지합니다. 빌드나 앱 실행 결과는 도구 검사 결과를 덮어쓰지 않습니다. 상시 감시가 아닌 과거 검사 결과입니다. 머신 설정이 바뀌면 다음 검사 전까지 이전 도구 결과를 표시하지 않습니다. 검사하지 않은 도구 구성을 준비 완료로 표시하지 않습니다.
2. Windows, Ubuntu, macOS 중 하나 이상을 선택할 수 있습니다. 환경 작업의 선택과 프로젝트별 빌드 대상은 서로 독립적입니다. 빌드할 때 선택한 모든 플랫폼이 하나의 소스 스냅샷을 사용합니다.
3. 설정은 진단과 필수 도구 준비를, 등록된 프로젝트를 선택한 화면은 빌드·선택적인 빌드 후 실행·최근 빌드 실행을 제공합니다. 등록한 프로젝트가 없어도 환경 작업을 수행할 수 있습니다. 각 작업은 인자 배열로 유지보수하는 CLI를 호출하며 별도 빌드 절차나 셸 문자열 보간을 만들지 않습니다.
4. 프로세스 출력을 스크롤 가능한 로그에 실시간 표시하고 제어 명령의 구조화된 결과 파일에서 플랫폼별 결과를 표시합니다. 실패한 단계를 찾을 수 있도록 명령, 프로세스 출력, 종료 코드를 디스크에 보존합니다. 도구 누락, 중지된 VM, 잘못된 프로젝트 경로, 결과 파일 누락, 0이 아닌 종료 코드는 실제 메시지를 포함한 실패로 표시합니다. 실행 오류가 발생한 게스트 명령은 자동 반복하지 않습니다. 완료 여부를 알 수 없을 때 설치나 빌드를 중복 실행하지 않아야 합니다.
5. 한 번에 한 작업을 실행합니다. 기존 제어 명령의 잠금이 터미널 명령과의 동시 실행도 보호합니다. 작업 중에는 정상적인 앱 종료를 막습니다. 게스트의 설치나 빌드를 안전하게 취소하는 기능이 있다고 표시하지 않습니다.
6. 폴더 선택, 플랫폼 선택, 실패 표시, 로그 표시는 데스크톱 API를 명시적으로 모의 처리한 브라우저에서 테스트합니다. 실제 Rust 명령 연결은 통제된 자식 프로세스와 실제 Ubuntu doctor 호출로 별도 테스트합니다. 네이티브 macOS 앱을 빌드하고 열어 화면을 확인합니다. 브라우저 모의를 네이티브 실행 증거로 간주하지 않고 네이티브 UI 자동화 범위를 정확히 기록합니다.
7. GUI 작업은 앱 데이터를 초기화하거나 도구 버전을 조용히 변경하거나 GitHub 워크플로를 실행하거나 릴리즈를 게시하지 않습니다. 미완성인 릴리즈 명령은 정상 동작하는 GUI 작업으로 표시하지 않습니다.

## 개발과 기록

GUI 소스는 `gui/`에 있습니다. `python3 desktop.py dev`는 화면 개발 서버와 Tauri Rust 개발 프로세스를 함께 실행합니다. `python3 desktop.py build --run`은 필요한 개발 도구를 준비하고 잠긴 GUI 의존성을 설치한 뒤 macOS 앱을 빌드하고 엽니다. 만들어진 앱은 이후 Python 명령을 입력하지 않고 실행할 수 있지만 내부 제어 명령에는 계속 Python이 필요합니다.

CLI는 `--os` 뒤에 여러 이름을 받고 기계가 읽을 결과를 위한 선택적 `--result-file`을 받습니다. 기존 단일 플랫폼 명령도 그대로 사용할 수 있습니다. GUI 설정은 Tauri 앱 설정 디렉터리에, 작업 로그와 결과는 제어 도구의 Git에서 제외한 `.state/` 디렉터리에 보관합니다.

첫 GUI, 명시적 프로젝트 등록, 프로젝트별 옵션, 공통 설정과 환경 결과 보존을 구현했습니다. 네이티브 검사와 아직 해결되지 않은 Parallels 실행 오류는 [GUI-VERIFICATION.ko.md](GUI-VERIFICATION.ko.md)에 기록합니다. 세 OS 릴리즈 검증은 [verification.md](verification.md)에 기록한 미완료 상태입니다.

참고: [Tauri 명령 연결](https://v2.tauri.app/develop/calling-rust/), [Tauri 채널](https://v2.tauri.app/develop/calling-frontend/), [네이티브 폴더 대화상자](https://v2.tauri.app/plugin/dialog/).
